---
vars:
  libvirt_uri: qemu+ssh://bjm/system
tasks:
  - name: define vm
    virt:
        name: obsd-build
        command: define
        xml: "{{ lookup('templates', 'libvirt.xml.j2') }}"
        uri: libvirt_uri
