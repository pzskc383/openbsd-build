DISTDIR=/data/distfiles
PLIST_REPOSITORY=/data/plist
PACKAGE_REPOSITORY=/data/packages
WRKOBJDIR=/obj

chroot=/build
chown_all=1
actions=unpopulate
actions=copy_ports
mkconf_lines=BULK_COOKIES_DIR=/obj/bulk
mkconf_lines=UPDATE_COOKIES_DIR=/obj/update
mkconf_lines=LOCKDIR=/obj/locks
mkconf_lines=LOGDIR=/obj/logs

